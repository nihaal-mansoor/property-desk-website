# Analytics, set per domain before launch. Client-visible by design.
PUBLIC_GA_ID=""

# Cloudflare Turnstile. Without the secret the endpoint rejects every
# submission by design: verifyTurnstile fails closed.
TURNSTILE_SECRET_KEY=""
PUBLIC_TURNSTILE_SITE_KEY=""

# Neon Postgres. The shared leads + rate_limit tables, one row per enquiry,
# tagged with source_domain so every site in the portfolio can share them.
DATABASE_URL=""
